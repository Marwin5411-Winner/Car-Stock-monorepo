<div class="header">
  <div class="header-logo">
    <img src="{{header.logoBase64}}" alt="Logo">
  </div>
  <div class="header-info">
    <div class="company-name-wrapper">
      <div class="company-name">{{header.companyName}}</div>
      <div class="header-line"></div>
      <div class="header-address">
        {{header.address1}}<br>
        {{header.address2}}<br>
        {{header.phone}}
      </div>
    </div>
  </div>
</div>

<div class="document-title">รายงานรายวันรับ</div>

<div class="text-center" style="margin-bottom: 20px;">
  <strong>ช่วงเวลา:</strong> {{dateRange}}
</div>

<div class="section">
  <table>
    <thead>
      <tr>
        <th style="width: 10%">วันที่เอกสาร</th>
        <th style="width: 12%">เลขที่เอกสาร</th>
        <th style="width: 12%">เลขที่ใบกำกับ</th>
        <th style="width: 16%">ชื่อลูกค้า</th>
        <th style="width: 14%">รายการ</th>
        <th style="width: 10%" class="text-right">จำนวนเงิน</th>
        <th style="width: 12%">ผู้รับ</th>
        <th style="width: 14%">หมายเหตุ</th>
      </tr>
    </thead>
    <tbody>
      {{#each payments}}
      <tr>
        <td>{{formatThaiDate paymentDate 'numeric'}}</td>
        <td>{{receiptNumber}}</td>
        <td>{{invoiceNumber}}</td>
        <td>{{customerName}}</td>
        <td>{{description}}</td>
        <td class="text-right">{{formatCurrency amount}}</td>
        <td>{{issuedBy}}</td>
        <td>{{notes}}</td>
      </tr>
      {{/each}}
      <tr style="font-weight: bold; background-color: #eee;">
        <td colspan="5" class="text-right">รวมทั้งสิ้น ({{summary.totalCount}} รายการ)</td>
        <td class="text-right">{{formatCurrency summary.totalAmount}}</td>
        <td colspan="2"></td>
      </tr>
    </tbody>
  </table>
</div>

<div class="footer" style="margin-top: 50px; text-align: right; font-size: 10px; color: #666;">
  พิมพ์เมื่อ: {{formatThaiDateWithDay (currentThaiDate)}}
</div>
